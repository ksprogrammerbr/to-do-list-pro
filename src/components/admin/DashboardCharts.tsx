interface ActivityData {
  date: string;
  userCount: number;
  taskCount: number;
  revenue: number;
}

// Componente para gráficos
const DashboardCharts = () => {
  // Integração com biblioteca de gráficos
  return <div className="mt-6">{/* Gráficos aqui */}</div>;
};
